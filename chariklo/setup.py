from setuptools import setup, find_packages

setup(
    name="chariklo",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Chariklo Team",
    description="Presence-based AI core for awareness."
)